// Firestore Security Rules yang direkomendasikan untuk development
// Paste rules ini ke Firebase Console > Firestore Database > Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access on all documents to any user
    // WARNING: These rules are for development only!
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Atau untuk production, gunakan rules yang lebih ketat:
    // match /articles/{articleId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    
    // match /users/{userId} {
    //   allow read: if true;
    //   allow write: if request.auth != null && request.auth.uid == userId;
    // }
  }
}